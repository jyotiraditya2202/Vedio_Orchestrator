def sfx_generator_orchestrator(state, task, original_prompt, video_specification):
    print("SFX GENERATOR ORCHESTRATOR")

    # later:
    # retrieve workers
    # select worker
    # execute worker

    return {
        "artifacts": {
            "sfx": None
        }
    }