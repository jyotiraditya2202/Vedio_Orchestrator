def dialogue_writer_orchestrator(state, task, original_prompt, video_specification):
    print("DIALOGUE WRITER ORCHESTRATOR")

    # later:
    # retrieve workers
    # select worker
    # execute worker

    return {
        "artifacts": {
            "dialogue": None
        }
    }
